# hooks/

Place Claude lifecycle hook scripts in this directory.

Hooks allow you to run custom scripts at specific points in the Claude Code lifecycle,
such as before or after tool calls, on session start/stop, etc.

See: https://docs.anthropic.com/en/docs/claude-code/hooks
