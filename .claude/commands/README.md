# commands/

Place custom Claude slash command definitions (`.md` files) in this directory.

Each file defines a custom `/command` that can be invoked inside a Claude Code session.

See: https://docs.anthropic.com/en/docs/claude-code/slash-commands
